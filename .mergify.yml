pull_request_rules:
  - name: Automatic merges for Renovate pull requests
    conditions:
      - author~=^renovate\[bot\]$
      - check-success=build
    actions:
      merge:
        method: squash
queue_rules: []
